# Reporting Package

This package owns the Phase 5 backend reporting baseline.

Current scope:

- markdown report assembly
- HTML report preview rendering
- review-aware report export
- printable report export for browser PDF generation
- reusable report rendering service

Printable export is stored as print-ready HTML so demos can save to PDF from the browser without a heavy renderer dependency.
